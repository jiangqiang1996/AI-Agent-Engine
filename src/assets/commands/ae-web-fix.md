---
description: 前端修复：视觉修复、交互修复、接口联调修复。以 DOM 结构化数据诊断为主，截图为辅。
model: $deep
subtask: false
argument-hint: "[问题描述] [url]"
---

使用 `@web-fix` 代理处理本次请求，任务描述：`$ARGUMENTS`

浏览器操作一律通过 `ae:playwright` 技能完成，不绕过该技能直接调用底层命令。
