---
lsmKind: design
upstreamRefs: []
traceTable:
  inputs: []
  outputs: []
splittingGuide:
  maxLines: 800
  strategy: 按模块或功能域分组 U-* 条目；索引文件保留设计概述和子文件索引；UI 设计子文件独立，超 800 行时按页面进一步拆分
  indexFile:
    required:
      - 概述
      - 子文件索引
    optional:
      - 现有架构约束
      - 技术选型决策
splittingRef: references/lsm-splitting-guide.md
trimmingGuide:
  required:
    - 设计概述
    - 实现映射（U-*，至少含需求映射+设计决策+接口边界+UI标记+UI设计引用+下游映射）
    - frontmatter.traceTable
  optional:
    - field: 现有架构约束
      condition: 项目已有架构模式或模块边界时保留
    - field: 技术选型决策
      condition: 存在多种可行方案需要记录选择理由时保留
    - field: 错误处理策略
      condition: 系统对错误处理有明确要求时保留
    - field: 性能约束
      condition: 有延迟/吞吐量/资源限制要求时保留
    - field: 依赖关系
      condition: 实现单元间存在依赖时保留
    - field: 状态管理策略
      condition: 实现单元涉及状态管理时保留
    - field: 数据模型
      condition: 实现单元涉及持久化或复杂数据结构时保留
    - field: 风险说明
      condition: 存在已知风险时保留
    - field: UI 设计子文件
      condition: 存在任意 U-* 标记为"涉及 UI"时保留；UI 子文件使用 references/ui-design-template.md 模板
---

# LSM Design: [项目/模块名称]

## 设计概述

[一段话描述整体设计思路和架构决策。必须包含：核心架构模式、关键设计权衡、与现有系统的集成方式。]

## 现有架构约束

<!-- trimmed: 项目已有架构模式或模块边界时保留 -->

- 架构模式：
- 模块边界：
- 依赖方向：
- 禁止事项：

## 技术选型决策

<!-- trimmed: 存在多种可行方案需要记录选择理由时保留 -->

| 决策项 | 选择 | 备选方案 | 选择理由 |
|--------|------|---------|---------|
|        |      |         |         |

## 错误处理策略

<!-- trimmed: 系统对错误处理有明确要求时保留 -->

- 错误传播方式：
- 重试策略：
- 降级策略：
- 日志记录要求：

## 性能约束

<!-- trimmed: 有延迟/吞吐量/资源限制要求时保留 -->

| 指标 | 目标值 | 度量方式 |
|------|--------|---------|
| 响应时间 |        |         |
| 吞吐量 |        |         |
| 资源限制 |        |         |

## 实现映射

### U-001: [实现单元标题]

**需求映射：** R-*

**设计决策：** [为什么这样设计。必须说明：解决了什么问题、权衡了什么、为什么不是其他方案。]

**接口边界：** [对外暴露的接口和协议。格式：方法签名 / API 路径 / 事件名 + 输入输出结构]

**数据模型：**

<!-- trimmed: 实现单元涉及持久化或复杂数据结构时保留 -->

```
[核心数据结构定义。使用目标语言的类型定义语法]
```

**依赖关系：**

<!-- trimmed: 实现单元间存在依赖时保留 -->

- 上游 U-ID：
- 下游 U-ID：

**状态管理策略：**

<!-- trimmed: 实现单元涉及状态管理时保留 -->

- 状态类型：无状态 / 有状态
- 持久化方式：
- 并发策略：

**风险说明：**

<!-- trimmed: 存在已知风险时保留 -->

- [已知风险]
- [缓解措施]

**UI 标记：** 涉及 UI / 不涉及 UI

**UI 设计引用：** [若 UI 标记为"涉及 UI"，填写 UI 子文件中的页面/组件映射 ID，如 P-001；若"不涉及 UI"则留空]

**下游映射：** TC-* / M-*

---

### U-002: [实现单元标题]

**需求映射：**

**设计决策：**

**接口边界：**

**数据模型：**

**依赖关系：**

**状态管理策略：**

**风险说明：**

**UI 标记：**

**UI 设计引用：**

**下游映射：**

## 子文件索引

<!-- 当设计文档拆分为子文件时，索引文件在此列出子文件；未拆分时删除本节 -->
<!-- 拆分规范见 references/lsm-splitting-guide.md，800 行硬限制 -->

| 文件名 | 范围 | 作用 |
|--------|------|------|
| <module>.md | [U-* ID 范围或模块名] | [该子文件承载的实现单元和作用] |
| ui-design.md | UI 设计体系与页面映射 | 存在 U-* 标记为"涉及 UI"时生成；使用 references/ui-design-template.md 模板 |
| ui-<scope>.md | [P-* ID 范围] | UI 设计超 800 行时按页面拆分；设计令牌在 index.md 定义，子文件引用 |
