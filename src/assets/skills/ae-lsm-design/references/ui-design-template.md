---
lsmKind: design-ui
upstreamRefs: []
traceTable:
  inputs: []
  outputs: []
splittingGuide:
  maxLines: 800
  strategy: 按页面或功能域分组 P-* 条目和组件规范；索引文件保留设计令牌和信息架构与导航
  indexFile:
    required:
      - 设计体系概述
      - 设计令牌
      - 信息架构与导航
      - 子文件索引
    optional: []
splittingRef: references/lsm-splitting-guide.md
trimmingGuide:
  required:
    - 设计体系概述
    - 设计令牌
    - 信息架构与导航
    - 页面映射（P-*，至少含实现单元映射+页面结构+关键交互）
    - frontmatter.traceTable
  optional:
    - field: 组件规范
      condition: 页面中存在可复用组件时保留
    - field: 交互状态矩阵
      condition: UI 组件有多状态（hover/focus/disabled/loading/error 等）时保留
    - field: 响应式策略
      condition: 需要适配多种屏幕尺寸时保留
    - field: 可访问性设计
      condition: 项目有可访问性合规要求时保留
    - field: 动效规范
      condition: UI 交互涉及过渡动画、微交互或加载动效时保留
    - field: 国际化设计
      condition: 项目需要多语言支持时保留
---

# UI Design: [项目/模块名称]

## 设计体系概述

[一段话描述项目 UI 设计的整体方向。必须包含：视觉风格定位（如简约/专业/活泼）、设计原则（最多 3 条）、与品牌/已有设计体系的关系。]

## 设计令牌

### 色彩

| 令牌名 | 色值 | 用途 |
|--------|------|------|
| --color-primary | [色值] | 主操作色、品牌色 |
| --color-primary-hover | [色值] | 主操作悬停态 |
| --color-primary-active | [色值] | 主操作按下态 |
| --color-secondary | [色值] | 次要操作色 |
| --color-success | [色值] | 成功状态 |
| --color-warning | [色值] | 警告状态 |
| --color-error | [色值] | 错误状态 |
| --color-info | [色值] | 信息提示 |
| --color-bg-primary | [色值] | 主背景色 |
| --color-bg-secondary | [色值] | 次背景色 |
| --color-bg-elevated | [色值] | 浮层/卡片背景 |
| --color-text-primary | [色值] | 主文本色 |
| --color-text-secondary | [色值] | 次文本色 |
| --color-text-disabled | [色值] | 禁用文本色 |
| --color-border | [色值] | 边框色 |
| --color-divider | [色值] | 分割线色 |

### 字体

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --font-family-base | [字体栈] | 正文 |
| --font-family-heading | [字体栈] | 标题 |
| --font-family-mono | [字体栈] | 代码/等宽 |

### 字号

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --font-size-xs | [如 12px] | 辅助文本 |
| --font-size-sm | [如 14px] | 正文小号 |
| --font-size-base | [如 16px] | 正文默认 |
| --font-size-lg | [如 20px] | 小标题 |
| --font-size-xl | [如 24px] | 标题 |
| --font-size-2xl | [如 32px] | 大标题 |

### 间距

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --space-xs | [如 4px] | 紧凑间距 |
| --space-sm | [如 8px] | 小间距 |
| --space-md | [如 12px] | 默认间距 |
| --space-lg | [如 16px] | 大间距 |
| --space-xl | [如 24px] | 区块间距 |
| --space-2xl | [如 32px] | 分区间距 |

### 圆角

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --radius-sm | [如 4px] | 按钮/标签 |
| --radius-md | [如 8px] | 卡片/输入框 |
| --radius-lg | [如 12px] | 弹窗/面板 |
| --radius-full | 50% / 9999px | 圆形/胶囊 |

### 阴影

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --shadow-sm | [如 0 1px 2px rgba(0,0,0,0.05)] | 轻微提升 |
| --shadow-md | [如 0 4px 6px rgba(0,0,0,0.1)] | 卡片悬浮 |
| --shadow-lg | [如 0 10px 15px rgba(0,0,0,0.15)] | 弹窗/下拉 |

### 断点

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --breakpoint-sm | [如 640px] | 移动端 |
| --breakpoint-md | [如 768px] | 平板端 |
| --breakpoint-lg | [如 1024px] | 桌面端 |
| --breakpoint-xl | [如 1280px] | 宽屏 |

## 信息架构与导航

### 全局导航结构

```
[项目全局导航树]
例：
├── 首页 (/)
├── 工作台
│   ├── 项目列表 (/projects)
│   └── 项目详情 (/projects/:id)
│       ├── 概览
│       ├── 成员
│       └── 设置
├── 消息中心 (/messages)
└── 个人设置 (/settings)
    ├── 基本信息
    └── 安全设置
```

### 导航模式

| 导航类型 | 适用位置 | 说明 |
|---------|---------|------|
| [如顶部导航] | [如全局] | [描述] |
| [如侧边导航] | [如二级页面] | [描述] |
| [如面包屑] | [如深层页面] | [描述] |

### 页面层级与流转

| 层级 | 页面 | 进入方式 | 返回方式 |
|------|------|---------|---------|
| L1 | [如首页] | 直接访问 | — |
| L2 | [如项目列表] | 导航菜单 | 导航菜单 |
| L3 | [如项目详情] | 列表点击 | 面包屑/返回 |

## 组件规范

<!-- trimmed: 页面中存在可复用组件时保留 -->

### C-001: [组件名称]

**组件类型：** [基础组件 / 业务组件 / 布局组件]

**变体：**

| 变体名 | 用途 | 视觉差异 |
|--------|------|---------|
| [如 primary] | [如主操作] | [如填充主色] |
| [如 secondary] | [如次要操作] | [如描边无填充] |
| [如 ghost] | [如最低优先级] | [如无边框] |

**尺寸：**

| 尺寸 | 高度 | 内边距 | 字号 |
|------|------|--------|------|
| sm | [px] | [px] | [令牌引用] |
| md | [px] | [px] | [令牌引用] |
| lg | [px] | [px] | [令牌引用] |

**关键样式约束：** [如最小宽度、最大行数、图标与文字间距等]

**使用场景：** [在哪些页面/组件中使用，使用规则]

---

### C-002: [组件名称]

**组件类型：**

**变体：**

**尺寸：**

**关键样式约束：**

**使用场景：**

## 页面映射

### P-001: [页面名称]

**实现单元映射：** U-*

**路由/入口：** [如 /dashboard，或"桌面端主窗口"]

**页面结构：**

```
[页面组件树，标注使用的设计令牌和组件]
例：
┌─────────────────────────────────────┐
│ 顶部导航 (C-Header)                 │
├──────────┬──────────────────────────┤
│ 侧边栏   │ 主内容区                  │
│ (C-Nav)  │ ┌──────────────────────┐ │
│          │ │ 筛选栏 (C-FilterBar)  │ │
│          │ ├──────────────────────┤ │
│          │ │ 列表 (C-List)         │ │
│          │ │  └─ 列表项 (C-Card)   │ │
│          │ └──────────────────────┘ │
│          │ 分页 (C-Pagination)       │
└──────────┴──────────────────────────┘
```

**关键交互：**

| 交互 | 触发 | 行为 | 反馈 |
|------|------|------|------|
| [如提交表单] | [如点击保存按钮] | [如校验并提交数据] | [如成功提示/错误高亮] |
| [如删除项] | [如点击删除] | [如确认弹窗→调用API] | [如列表移除动画] |
| [如筛选] | [如切换筛选条件] | [如重新请求数据] | [如列表加载态] |

**页面数据来源：** [接口/API/状态，与接口边界的对应关系]

**特殊布局约束：** [如固定侧边栏宽度、最小内容区宽度等]

---

### P-002: [页面名称]

**实现单元映射：**

**路由/入口：**

**页面结构：**

**关键交互：**

**页面数据来源：**

**特殊布局约束：**

## 交互状态矩阵

<!-- trimmed: UI 组件有多状态（hover/focus/disabled/loading/error 等）时保留 -->

### 全局交互规范

| 状态 | 视觉表现 | 适用范围 |
|------|---------|---------|
| hover | [如背景色提升一级] | 所有可交互元素 |
| focus | [如 2px 主色描边] | 所有可聚焦元素 |
| active/pressed | [如背景色加深] | 按钮/链接 |
| disabled | [如透明度 0.4 + 禁止光标] | 表单控件/按钮 |
| loading | [如骨架屏/旋转指示器] | 异步加载区域 |
| error | [如红色边框 + 错误文本] | 表单字段 |
| empty | [如插图 + 引导文案] | 列表/数据区域 |

### 页面级加载策略

| 页面 | 加载方式 | 骨架屏 | 空状态 |
|------|---------|--------|--------|
| [P-001] | [整体/分块] | [是/否] | [空状态描述] |

## 响应式策略

<!-- trimmed: 需要适配多种屏幕尺寸时保留 -->

### 断点布局变化

| 断点 | 整体布局 | 导航变化 | 内容区变化 | 隐藏/新增组件 |
|------|---------|---------|-----------|-------------|
| 移动端 (<640px) | [如单列堆叠] | [如底部Tab] | [如全宽] | [如隐藏侧边栏，新增筛选按钮] |
| 平板端 (640-1023px) | [如两栏] | [如可折叠侧边栏] | [如内容区自适应] | |
| 桌面端 (≥1024px) | [如三栏固定] | [如固定侧边栏] | [如最大宽度约束] | |

### 页面级响应式

| 页面 | 移动端布局 | 平板端布局 | 桌面端布局 |
|------|-----------|-----------|-----------|
| P-001 | [描述] | [描述] | [描述] |

## 可访问性设计

<!-- trimmed: 项目有可访问性合规要求时保留 -->

- 目标标准：[如 WCAG 2.1 AA]
- 键盘导航：[Tab 顺序约定、焦点陷阱处理、快捷键]
- ARIA 策略：[角色标注规范、标签生成规则、实时区域标注]
- 颜色对比度：[文本对比度要求、非文本对比度要求]
- 屏幕阅读器：[阅读顺序约定、忽略区域、动态内容播报策略]
- 减少动画：[是否尊重 prefers-reduced-motion，降级策略]

## 动效规范

<!-- trimmed: UI 交互涉及过渡动画、微交互或加载动效时保留 -->

### 动效令牌

| 令牌名 | 值 | 用途 |
|--------|-----|------|
| --duration-fast | [如 150ms] | 微交互（按钮反馈、开关） |
| --duration-normal | [如 250ms] | 常规过渡（展开/折叠、淡入） |
| --duration-slow | [如 400ms] | 复杂过渡（页面切换、弹窗） |
| --easing-default | [如 ease-in-out] | 默认缓动 |
| --easing-in | [如 ease-in] | 退场 |
| --easing-out | [如 ease-out] | 入场 |

### 场景动效

| 场景 | 类型 | 时长令牌 | 缓动令牌 | 说明 |
|------|------|---------|---------|------|
| 页面切换 | 过渡 | --duration-slow | --easing-out | |
| 弹窗出现 | 入场 | --duration-normal | --easing-out | |
| 弹窗关闭 | 退场 | --duration-fast | --easing-in | |
| 列表项加载 | 骨架屏/渐入 | --duration-normal | --easing-out | |
| 按钮反馈 | 微交互 | --duration-fast | --easing-default | |
| 侧边栏展开 | 过渡 | --duration-normal | --easing-default | |

### 动效降级

[是否尊重 prefers-reduced-motion，降级时哪些动效省略、哪些替换为即时切换]

## 国际化设计

<!-- trimmed: 项目需要多语言支持时保留 -->

- 文本方向：[LTR / RTL / 双向]
- 文本溢出策略：[截断/换行/自适应宽度]
- 日期/数字格式：[如中文 yyyy-MM-dd，数字千分位逗号]
- 布局弹性约束：[如按钮最小宽度、标签与输入框的间距是否随文本长度自适应]
