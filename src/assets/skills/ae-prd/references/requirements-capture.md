# 需求捕获

此内容在阶段 3 开始时加载——在协作对话产生了值得保留的持久决策之后。

---

此文档是给 AI 工作流使用的需求数据文档，不是 PRD 汇报材料。只包含规划需要良好执行的内容，跳过对范围没有价值的章节。

需求数据文档用于产品定义和范围控制。**不要**包含实现细节（库、Schema、端点、文件布局），除非需求讨论本身是技术性的且这些细节是决策的主题。

需求文档同时面向人读和 AI 工作流。结构应偏向稳定、显式、可解析：保留固定章节名、稳定 ID、验收条件、范围边界和待定问题分类。文档正文必须自包含。

format: human-readable-requirements

**分片规则：** 需求文档以目录形式产出时，采用 `index.md` + `global.md` + `modules/<NN>-<m>/module.md` + `modules/<NN>-<m>/prototype.md`（仅 involvesUI=true 时）三层结构。每个模块位于 `modules/<NN>-<m>/` 子目录（`<NN>` 为零填充数字序号如 01、02、03），子目录名带数字固定顺序。模块文件采用 section anchor 分区（`##` 章节带 `{#anchor}`）。`module.md` 的 frontmatter 声明 `type: prd-module`、`parent:`（指向 index.md）和 `module:`（模块名）；`prototype.md` 的 frontmatter 声明 `type: prd-prototype`、`parent:`（指向 index.md）和 `module:`（模块名）。各文件超行数上限时禁止压缩内容，按数字顺序分片（如 `module-1.md`、`module-2.md`...；`prototype-1.md`、`prototype-2.md`...；`global-1.md`、`global-2.md`...），每片在 `##` 章节边界切分保持片内语义完整，分片文件名数字固定顺序。`prototype.md` 仅当模块 `involvesUI=true` 时产出，不存在即表示该模块不涉及 UI。

**省略没有实质内容的可选章节**，避免生成无内容占位章节。

**非简单工作的必需内容：**
- 问题框架
- 带有稳定 ID 的具体需求或预期行为
- 范围边界
- 成功标准

**在有实质帮助时包含：**
- 关键决策及其理由
- 依赖或假设
- 待定问题
- 考虑过的替代方案

## 产物结构

需求文档以目录形式产出：

```
ae/prds/<topic>-YYYY-MM-DD/
├── index.md                        # L0 索引（自动生成，无行数限制）
├── global.md                       # 全局共识（单文件，section anchor 分区，≤ 300 行）
└── modules/
    └── <NN>-<module-name>/         # 模块子目录（数字序号 + 模块名）
        ├── module.md               # §需求 + §成功标准（≤ 300 行）
        └── prototype.md            # §原型 + §响应式布局（≤ 300 行，仅 involvesUI=true 时存在）
```

**小项目模式**（<3 模块且无跨模块依赖）：
```
ae/prds/<topic>-YYYY-MM-DD/
├── index.md                        # L0 索引（自动生成，无行数限制）
└── prd.md                          # 全局 + 唯一模块合并单文件（≤ 300 行）
```

## index.md 模板（自动生成）

```markdown
---
type: prd-index
autoGenerated: true
generatedAt: "YYYY-MM-DDTHH:MM:SSZ"
sourceHash: "<所有文件 contentHash 的聚合>"
version: "1.0"
---

# <主题标题>

## 摘要
<一句话问题描述> · <N>个模块 · <N>条需求

## 模块导航
| 模块 | 描述 | 涉及 UI | 需求 IDs（当前态） | 文件 |
|------|------|--------|------------------|------|
| auth | 用户认证 | 是 | R1-R5 | modules/01-auth/module.md, modules/01-auth/prototype.md |
| resource | 资源管理 | 否 | R6-R12 | modules/02-resource/module.md |

## 全局导航
| 章节 | Anchor | IDs | 摘要 |
|------|--------|-----|------|
| 问题框架 | {#problem} | - | 用户目标、痛点 |
| 范围边界 | {#scope} | - | In/Out 边界 |
| 跨模块决策 | {#decisions} | D1-D3 | 关键决策 |

## ID 索引（当前态）
| ID | 类型 | 模块 | 文件 | Anchor |
|----|------|------|------|--------|
| R1 | requirement | auth | modules/01-auth/module.md | {#requirements} |

## 跨模块依赖
| 源模块 | 目标模块 | 依赖类型 | 引用 IDs |
|--------|---------|---------|---------|
| auth | resource | API 调用 | R5 -> R6 |

## 变更历史摘要
（如有变更记录，列出最近变更；无变更时省略此章节）
```

## global.md 模板

```markdown
---
type: prd-global
parent: "index.md"
ids: [D1, D2, D3, NFR1, NFR2]
---

## 问题框架 {#problem}

[用户目标、当前痛点、成功判据]

## 范围边界 {#scope}

### In Scope
- 用户认证与授权
- 资源 CRUD

### Out Scope
- 社交功能
- 支付集成

### 模块清单
| 模块 | 边界声明 | 可调整 |
|------|---------|--------|
| auth | 认证域，不包含权限策略 | 是 |
| resource | 资源域，依赖 auth 的会话 | 是 |

## 跨模块决策 {#decisions}

### D1: 认证使用 JWT
理由: 用户需求明确提及"JWT 认证"

## 全局非功能 {#non-functional}

### NFR1: 登录响应 < 500ms

## 技术栈约束（用户指定）{#tech-stack}

> 仅记录用户在需求中明确提及的技术栈，不主动追问。无用户指定技术栈时省略此章节。

- 前端: React（用户明确指定）
- 后端: Spring Boot（用户明确指定）

## 设计愿景（仅当涉及 UI）{#design-vision}

### 主题色
- 主色: #1890ff
- 次要颜色:
  - 背景色: #ffffff
  - 文本色: #333333
  - 边框色: #d9d9d9
  - 次要文本色: #999999
  - 强调色: #52c41a
- 色调: 冷色
- 视觉风格关键词: 极简

### 响应式声明
- 是否响应式: 否（默认，用户未提及）
- 说明: 未要求响应式，固定布局
```

## modules/\<NN>-\<m\>/module.md 模板

```markdown
---
type: prd-module
parent: "index.md"
module: auth
involvesUI: true
ids: [R1, R2, R3, R4, R5]
dependsOn: [resource]
dependedBy: [audit]
changes: []
---

## 需求 {#requirements}

### R1: 用户注册
用户可使用邮箱和密码注册账号。密码需至少 8 位含大小写字母和数字。
- -> 验收: SC1

### R2: 用户登录
已注册用户可使用邮箱和密码登录。
- -> 验收: SC2
- 依赖: R1

## 成功标准 {#success-criteria}

### SC1: 注册成功后用户存在于系统中
### SC2: 登录成功后返回有效会话令牌
```

## modules/\<NN>-\<m\>/prototype.md 模板（仅 involvesUI=true 时产出）

```markdown
---
type: prd-prototype
parent: "index.md"
module: auth
ids: [PAGE-001, PAGE-002, PAGE-003]
---

## 原型 {#prototype}

### PAGE-001: 注册页面
- 路由: /register
- 布局结构: 居中卡片，含邮箱输入、密码输入、确认密码、注册按钮
- 交互流程: 点击注册按钮 → 提交表单 → 成功跳转 /login
- 页面元素清单:
  | 元素 | 类型 | 必填 | 验证规则 | 区域 |
  |------|------|------|---------|------|
  | 邮箱 | email input | 是 | 合法邮箱格式 | 中部 |
  | 密码 | password input | 是 | 至少8位含大小写字母和数字 | 中部 |
  | 注册按钮 | submit button | — | — | 底部 |

## 响应式布局 {#responsive}

> 仅当 global.md §设计愿景声明为响应式时产出此章节。非响应式时省略，响应式声明集中在 global.md §设计愿景一次性声明。

[各断点下的布局变化描述]
```

## Frontmatter 字段说明

### index.md

| 字段 | 值 | 说明 |
|------|-----|------|
| `type` | `prd-index` | 固定值 |
| `autoGenerated` | `true` | 自动生成标记 |
| `generatedAt` | ISO 时间戳 | 生成时间 |
| `sourceHash` | 哈希值 | 所有文件 contentHash 的聚合 |
| `version` | `"1.0"` | 版本号 |

### global.md

| 字段 | 值 | 说明 |
|------|-----|------|
| `type` | `prd-global` | 固定值 |
| `parent` | `"index.md"` | 指向索引文件 |
| `ids` | ID 数组 | 全局定义的所有稳定 ID |

### modules/\<NN>-\<m\>/module.md

| 字段 | 值 | 说明 |
|------|-----|------|
| `type` | `prd-module` | 固定值 |
| `parent` | `"index.md"` | 指向索引文件 |
| `module` | 模块名 | kebab-case 模块名 |
| `involvesUI` | `true`/`false` | 是否涉及 UI |
| `ids` | ID 数组 | 模块定义的稳定 ID（R、SC） |
| `dependsOn` | 模块名数组 | 依赖的模块 |
| `dependedBy` | 模块名数组 | 被依赖的模块 |
| `changes` | 变更数组 | 变更记录（初始为空数组） |

### modules/\<NN>-\<m\>/prototype.md

| 字段 | 值 | 说明 |
|------|-----|------|
| `type` | `prd-prototype` | 固定值 |
| `parent` | `"index.md"` | 指向索引文件 |
| `module` | 模块名 | kebab-case 模块名 |
| `ids` | ID 数组 | 原型定义的稳定 ID（PAGE） |

## superseded_by 变更追踪

需求条目变更时使用 `superseded_by` 软删除，保留历史：

1. 旧记录保留，标记 `supersededBy: <新ID>`，不从文件中删除
2. 新记录添加 `supersedes: <旧ID>` 链接
3. 默认读取只返回 `supersededBy` 为 null 的记录（当前态）
4. 单向箭头，允许链式（R1→R1.v2→R1.v3），禁止环
5. Superseded 记录用 HTML 注释隐藏：`<!-- ### R1.v1: 用户注册（已废弃） -->`

### changes 数组格式

```yaml
changes:
  - id: change-001
    date: "2026-07-24"
    type: modify          # modify | add | remove
    targetId: R1
    fromVersion: R1.v1
    toVersion: R1
    changeSummary: "密码强度要求增强"
    affectedDimensions: [api, database, ui-ux, test-cases]
    affectedIds: [EP-001, T-users, PAGE-001, ST-001, TC-001]
    blastRadius: 2
    reason: "用户反馈密码强度不足"
```

## 稳定 ID 体系

| ID 前缀 | 类型 | 示例 |
|---------|------|------|
| R | 需求条目 | R1, R2 |
| SC | 成功标准 | SC1, SC2 |
| D | 决策 | D1, D2 |
| NFR | 非功能需求 | NFR1 |
| PAGE | 页面 | PAGE-001 |

## 原型设计

### 定位

原型设计是 ae:prd 的组成部分，用于在需求阶段确定产品逻辑层的确定性边界。**不是最终页面设计**。最终页面详细设计（HTML 结构、CSS 样式、组件选型、技术栈）属于 ae:design 的 ui-ux 维度。

原型设计用 Markdown 描述页面结构和交互，产出在各模块的 `modules/<NN>-<m>/prototype.md` 独立文件中。仅当模块 `involvesUI=true` 时产出。

### 技术栈隔离规则（硬约束）

原型设计产物（`modules/<NN>-<m>/prototype.md`）**禁止出现任何前端开发技术栈或第三方依赖名称**。页面产物只描述**产品逻辑层语义**：页面要做什么、长什么样、怎么交互。技术栈信息集中收录到 `global.md` §技术栈约束。

### 确定性边界

原型必须明确锁定**产品逻辑层**的所有确定性内容：

**必须确定的：**
- 页面数量和页面列表
- 每页面的路由
- 导航结构
- 主题色声明（HEX 值，集中在 global.md §设计愿景）
- 响应式声明
- 每页面的布局结构
- 每页面的核心交互流程
- 页面间跳转关系
- 表单字段清单和必填规则

**允许差异的：**
- 具体间距、字号、圆角数值
- 字体选择
- 动画效果
- 图标样式

### 原型与 ae:design ui-ux 的关系

| | ae:prd 原型 | ae:design ui-ux |
|--|-----------|-----------------|
| **目的** | 确定产品逻辑层确定性边界 | 最终页面详细设计，供 ae:work 实施 |
| **位置** | modules/\<NN>-\<m\>/prototype.md | design modules/\<NN>-\<m\>/ui-ux.md |
| **技术栈** | **禁止出现** | 集中声明在 design global.md |
| **消费者** | 用户（验证产品逻辑确定性） | LLM（生成实现代码） |

## 行数限制

| 文件 | 限制 | 超限处理 |
|------|------|---------|
| index.md | 无行数限制 | - |
| global.md | ≤ 300 行硬上限 | 禁止压缩，按数字顺序分片为 `global-1.md`、`global-2.md`、...，每片 ≤ 300 行 |
| modules/\<NN>-\<m\>/module.md | ≤ 300 行硬上限 | 禁止压缩，按数字顺序分片为 `module-1.md`、`module-2.md`、...，每片 ≤ 300 行 |
| modules/\<NN>-\<m\>/prototype.md | ≤ 300 行硬上限 | 禁止压缩，按数字顺序分片为 `prototype-1.md`、`prototype-2.md`、...，每片 ≤ 300 行 |

分片在 `##` 章节边界切分，保持片内语义完整。分片文件名数字固定顺序，`index.md` 记录所有分片文件清单。

**所有文件生成时即保证不超限，不产出大文件再后置拆分。超限时禁止压缩内容，必须按数字顺序分片。**

## 完整性检查

在最终确定之前，检查：
- 如果这个需求探索现在结束，`ae:design` 还需要发明什么？
- 是否有任何需求依赖于声称在范围之外的东西？
- 是否有任何未解决的项目实际上是产品决策而非规划问题？
- 是否有不应该出现的实现细节泄漏了进来？
- 是否有不应该出现的汇报型内容泄漏了进来？
- 是否有任何需求声称基础设施不存在而该声明尚未对照代码库验证？
- 每个需求条目是否都有明确的验收条件？
- 是否保留了足够稳定 ID，使 `ae-doc-extract` 可按 ID 或模块提取所需上下文？
- `global.md` 的模块清单与实际产出的 `modules/` 文件是否一致？

如果规划需要发明产品行为、范围边界或成功标准，需求探索还没有完成。

在写入之前确保 `ae/prds/` 目录存在。需求文档以目录形式产出，目录名为 `<topic>-YYYY-MM-DD`。

## 视觉辅助

视觉辅助以内容模式为条件，而非以深度分类为条件。

**何时包含：**

| 需求描述的是... | 视觉辅助 | 放置位置 |
|---|---|---|
| 多步骤用户工作流或流程 | Mermaid 流程图（`flowchart`） | 在 global.md §问题框架之后 |
| 3+ 种行为模式、变体或状态 | Markdown 比较表 | 在 modules/\<NN>-\<m\>/module.md §需求内 |
| 3+ 个交互参与者 | Mermaid 关系图 | 在 global.md §问题框架之后 |
| 多个竞争方案 | 比较表 | 在阶段 2 方案探索中 |

**格式选择：** Mermaid（默认）用于流程图、关系图和时序图；Markdown 表格用于模式/变体比较。文字是权威的：视觉辅助与文字不一致时以文字为准。
