---
lsmKind: test
upstreamRefs: []
traceTable:
  inputs: []
  outputs: []
trimmingGuide:
  required:
    - 测试概述
    - 测试用例（TC-*，至少含需求映射+测试步骤+预期结果+测试类型+优先级）
    - frontmatter.traceTable
  optional:
    - field: 测试数据准备
      condition: 测试需要特定数据集或数据状态时保留
    - field: 断言类型
      condition: 除简单等值断言外有特殊断言需求时保留
    - field: 自动化可行性
      condition: 部分用例可能无法自动化时保留；若保留则不可自动化原因也必须保留
    - field: 不可自动化原因
      condition: 自动化可行性为非"可自动化"时必须保留；可自动化时必须删除
    - field: 设计映射
      condition: 有设计文档时可保留以增强追溯
    - field: 前置条件
      condition: 测试有明确前置状态要求时保留
---

# LSM Test Cases: [项目/模块名称]

## 测试概述

[一段话描述测试覆盖的整体情况。必须包含：覆盖了哪些需求、测试类型分布、未覆盖场景。]

## 测试用例

### TC-001: [测试用例标题]

**需求映射：** R-*

**设计映射：** U-*

<!-- trimmed: 有设计文档时可保留以增强追溯 -->

**测试类型：** 正常路径 / 边界条件 / 异常路径 / 集成场景

**优先级：** P0 / P1 / P2

**前置条件：**

<!-- trimmed: 测试有明确前置状态要求时保留 -->

- [执行前必须满足的环境/数据/状态条件]

**测试数据准备：**

<!-- trimmed: 测试需要特定数据集或数据状态时保留 -->

| 数据项 | 值/生成方式 | 用途 |
|--------|-----------|------|
|        |           | |

**测试步骤：**

| 步骤 | 操作类型 | 目标 | 操作详情 | 预期结果 |
|------|---------|------|---------|---------|
| 1 | API调用 / UI操作 / 命令执行 / 数据操作 | [操作的对象] | [具体操作] | [该步骤的预期输出/状态] |
| 2 | | | | |

**断言类型：**

<!-- trimmed: 除简单等值断言外有特殊断言需求时保留 -->

- 等值断言 / 包含断言 / 异常断言 / 视觉断言 / 性能断言 / 状态断言

**自动化可行性：** 可自动化 / 需人工介入 / 仅人工

<!-- trimmed: 部分用例可能无法自动化时保留 -->

**不可自动化原因：**

<!-- trimmed: 自动化可行性为非"可自动化"时必须保留；可自动化时必须删除 -->

---

### TC-002: [测试用例标题]

**需求映射：**

**设计映射：**

**测试类型：**

**优先级：**

**前置条件：**

**测试数据准备：**

**测试步骤：**

**断言类型：**

**自动化可行性：**

**不可自动化原因：**
