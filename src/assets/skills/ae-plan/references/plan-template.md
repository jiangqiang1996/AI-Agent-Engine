# AI 结构化计划模板

本模板是 `ae:plan` 和 `ae:refactor` 生成 `type: plan` 文档的唯一结构契约。

生成计划时必须保持正文章节名、字段名和实现单元结构稳定。frontmatter 可选字段按规则省略；可选章节仅在有实质内容时包含；不得写“暂无”“待补充”等占位内容。计划是给 AI 执行的结构化文档，不需要干系人、读者对象、业务价值、背景介绍或汇报型影响说明；只有这些信息会改变实现、验证、迁移、发布或回滚决策时，才可作为范围、约束或风险记录。

```markdown
---
type: plan
status: drafted
date: YYYY-MM-DD
title: <kebab-case-title>
origin: <上游产物路径，若无则删除此行>
originFingerprint: <上游指纹，若无则删除此行>
depth: <standard|deep>  # 轻量计划删除此行
---

# <标题>

## AI 解析契约
- canonicalKind: plan
- humanEquivalent: true
- stableIdsRequired: true
- implementationUnitsRequired: true
- noImplicitScope: true

## 来源与目标
[来源、目标、非目标和外部行为保持要求。可记录上游路径，但不得用上游路径替代具体内容]

## 范围

### 包含
- <范围内事项>

### 不包含
- <范围外事项>

### 约束 [可选]
- <业务、技术、时间或资源约束>

## 需求追溯
| 需求 ID | 计划响应 |
|---------|----------|
| R1 | U1 |

## 高层技术设计
[架构、数据流、状态流或关键设计说明。Mermaid 图可原样包含]

### 关键决策
- D1. <决策> → 理由: <理由>

## 专项设计 [可选]

### 数据库设计 [可选]
[表、字段、索引、迁移策略、回填和回滚信号]

### 接口设计 [可选]
[接口路径、请求、响应、错误语义、版本兼容]

### 数据模型 [可选]
[模型、DTO、实体、字段约束]

### 安全设计 [可选]
[认证、授权、数据保护、输入校验]

### 性能设计 [可选]
[缓存、并发、复杂度、I/O、预算]

### 部署与回滚 [可选]
[部署步骤、配置、回滚信号和回滚方案]

## 实现单元

### U1. <单元标题>
- [ ] 目标: <目标>
- [ ] 覆盖需求: R1, R2
- [ ] 行为保持要求: <重构计划必填；普通计划按需填写>
- [ ] 依赖: <依赖单元或前置条件>
- [ ] 文件:
  - <文件路径>
- [ ] 方法:
  - <实现方案>
- [ ] 需遵循的模式:
  - <模式或既有实现参考>
- [ ] 测试场景:
  - 正常路径: <测试>
  - 边界情况: <测试>
  - 错误路径: <测试>
  - 集成场景: <测试>
- [ ] 验证:
  - <验证命令或验证方式>
- [ ] 回滚信号 [可选]: <信号>

## 风险与应对 [可选]
| 风险 | 影响 | 应对措施 |
|------|------|----------|
| <风险> | <影响> | <措施> |

## 待定问题 [可选]

### 执行前需解决
- Q1. <问题>

### 推迟到执行
- Q2. <问题>

## 一致性检查
- implementationUnitsCount: <U* 数量>
- tracedRequirementsCount: <被追溯的 R* 数量>
- decisionsCount: <D* 数量>
- risksCount: <风险数量>
```

## 兼容要求

- `ae:plan` 和 `ae:refactor` 生成的 `type: plan` 文档必须符合此结构。
- `ae:work` 必须能直接读取“实现单元”章节执行工作，不需要阅读人读详细设计文档。
- `origin` 和 `originFingerprint` 只在有真实上游文档时填写；不得用上游路径替代计划正文中的可执行内容。
- 可选章节缺失不代表 TODO；只有“待定问题”中的问题才表示未决。
- 本计划模板必须同时人读清晰且机器可抽取：不得为了“完整性”新增计划中没有的干系人、业务价值、背景介绍、影响叙述或最佳实践章节。
