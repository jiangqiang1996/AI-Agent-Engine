openapi: 3.0.0
info:
  title: Unsafe Ref API
paths:
  /users:
    get:
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                $ref: '../swagger-2-basic.json#/definitions/Order'
