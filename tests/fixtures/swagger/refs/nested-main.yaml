openapi: 3.0.0
info:
  title: Nested Ref API
  version: 1.0.0
paths:
  /users:
    get:
      summary: List users
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './nested/first.yaml#/components/schemas/User'
