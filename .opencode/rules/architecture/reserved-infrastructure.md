# 预置基础设施与未调用函数

> 本文件由 `/ae-save-rules` 命令生成，最后更新：2026-04-21

## 预置基础设施与未调用函数

项目中存在为未来扩展预留的基础设施函数（如 `syncAgentAssets`），当满足以下条件时不应报告为问题：
- 函数有明确的职责定义和完整实现
- 对应的测试文件已编写并通过
- 当前未被调用是因为上游机制（如 opencode 的 agent 发现路径）绕过了该函数
- 函数是为将来可能启用的功能路径预留的

判断方法：先确认当前的实际调用路径，再判断未调用是否是预留状态而非遗漏。
