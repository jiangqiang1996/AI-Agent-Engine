const http = require('http')

const TOKEN = process.argv[2]
const title = '中文去 AI 味通用方法：从 GitHub 三个开源 Skill 提炼的可执行清单'
const content = `## 为什么需要这篇文章

AI 生成的中文有一组可辨识的统计惯性。单独看每条都不算错，堆叠起来就是「AI 味」。GitHub 上围绕这件事已经出现几个热度较高的开源 Skill，各自面向不同场景，但底层方法高度收敛。

本文从目前 stars 最高的三个项目里提炼出一份通用清单，不绑定任何特定平台，可以直接拿去用。

## 三个项目的定位

| 项目 | stars | 平台 | 定位 |
|------|------|------|------|
| mengke-wang/zh-humanizer-literary | 70 | Codex | 通用中文去 AI 味 + 文采增强 |
| allenloves/de-ai-tone | 62 | Claude Code | 繁体中文写作与翻译风格规范 |
| zczjyq/de-AIGC-skill | 24 | Cursor | 学术论文降 AIGC 检测率 |

三个项目覆盖了通用文案、翻译润色、学术论文三个典型场景，合在一起基本能代表当前中文去 AI 味开源方案的主流做法。

## 一、AI 味的典型信号（先识别再处理）

三个项目都先列出了可观测的 AI 写作惯性，而不是靠感觉润色。归纳起来：

### 结构层信号

- 假对比：「不是 X 而是 Y」的假辩证，频繁出现且并非真正转折
- 三项式排比：凡事都凑三条，每条长度接近
- 段末微型总结：每段结尾都收一句「综上所述」「这意味着」
- 开场公式：「在当今时代」「随着技术的发展」「值得注意的是」
- 结构强迫症：滥用列点、加粗、小标题，即使内容不需要

### 词汇层信号

- 空转话语标记：「值得注意的是」「综上所述」「不难发现」
- 贴标签词汇：「赋能」「打造」「旨在」「助力」「深耕」
- 赘词「一个」：英文 article 的硬译，中文不需要
- 框式介词：「在...方面」「在...的过程中」从英文直译

### 句法层信号

- 系动词堆砌：反复用「是」「有」开头，回避动词
- 高度对称并列：两个分句结构完全镜像
- 破折号滥用：用破折号代替句号和逗号
- 修辞性设问：自问自答凑节奏

### 学术专属信号

- 泛化结论：「具有重要意义」「为...提供了借鉴」
- 模板化「核心问题」陈述
- 模糊归因：「研究表明」「实践证明」不带出处
- 「首先/其次/再次」编号逻辑
- 「此案例印证了」段末套路
- 「依据/基于 XX 理论」起笔

## 二、通用处理原则（三个项目的共识）

### 原则 1：每个修辞手段都必须挣回自己的位置

这是 de-ai-tone 的核心原则，另外两个项目事实上也遵循。不是一刀切禁用，而是要求每个修辞都能说明为什么必须在这里出现。说不通就删。

### 原则 2：抽象判断换成具体的人、场景、动作、后果

zh-humanizer-literary 把这条放在首位。与其说「提升了用户体验」，不如说「加载时间从 3 秒降到 0.8 秒，首屏跳出率下降 40%」。没有具体数据就说具体动作，没有具体动作就承认信息不足，不要用抽象判断填充。

### 原则 3：保留有用结构，去掉模板感

结构本身不是敌人。真正需要的信息用列点清晰，不需要的信息用列点反而暴露 AI 味。判断标准：如果去掉列点改成连续句子，读者会不会觉得更自然？会就改。

### 原则 4：区分事实、推断和建议

三个项目都强调不编造案例、数字或结果。AI 味的一个来源是用编造的「案例」填充论证。改写时保留原文的事实边界，推断就标推断，建议就标建议。

### 原则 5：短句节奏 + 动词驱动

de-ai-tone 的正向原则。中文好句子靠动词推进，不靠系动词和名词短语堆砌。长短句交替产生节奏，全长句或全短句都显刻意。

### 原则 6：信任读者

不要每段结尾替读者总结。说清楚事实，读者能自己得出结论。过度总结等于不信任读者，也是 AI 味的核心来源之一。

## 三、可执行清单（拿来就能用）

把上面原则落到具体操作，按顺序执行。

### 第 1 步：扫描信号词

建立一份信号词表，全文搜索命中次数。三个项目都提供了各自的词表，合并去重后核心词如下：

\`\`\`
在当今时代、随着技术的发展、值得注意的是、综上所述、不难发现、
赋能、打造、旨在、助力、深耕、赋能、生态、闭环、
具有重要意义、为...提供了借鉴、此案例印证了、
首先、其次、再次、最后、
不是...而是、一方面...另一方面
\`\`\`

命中就标记，逐条判断是否删除。多数情况下直接删，信息不丢失。

### 第 2 步：拆三项式

搜索连续三个结构相近的短语或句子。如果三条都承载实质信息，保留；如果第三条是凑数，删第三条；如果三条都在说同一件事，合并成一句。

### 第 3 步：去段末总结

每段最后一句如果是「这意味着」「综上」「可以说」开头的总结句，判断该段是否真的需要总结。多数情况删掉总结句，段落信息不丢失，反而更干净。

### 第 4 步：破折号和设问

破折号换成句号或逗号。修辞性设问（自问自答）改成陈述句，或者只保留问句删掉自己作答的部分。

### 第 5 步：抽象判断具体化

搜索「提升了」「优化了」「加强了」「改善了」这类动词，检查后面有没有具体对象和可观测变化。没有就补，补不了就承认信息不足并删掉空判断。

### 第 6 步：列点审查

每个列点块问一遍：这些项之间是并列关系还是只是凑在一起？去掉列点改成连续句子会不会更清楚？是就改写成句子。真正并列且每项都承载信息才保留列点。

### 第 7 步：开场和收场

删掉所有公式化开场。文章直接从第一个事实或第一个观点开始。结尾不要「总之」「综上」「让我们」，停在最后一个实质内容上。

### 第 8 步：术语和前后一致

这是 de-AIGC-skill 的特色。长文改写时维护一份术语表，后文不要重复释义，也不要前后用不同译名。学术场景尤其重要，通用场景也适用。

### 第 9 步：自检清单

产出前强制过一遍：

- [ ] 信号词是否清零或大幅减少
- [ ] 是否还有三项式排比
- [ ] 每段结尾是否还在替读者总结
- [ ] 抽象判断是否都补了具体依据
- [ ] 列点是否都经得起「改句子」测试
- [ ] 开场结尾是否还有公式
- [ ] 术语是否前后一致

## 四、场景差异处理

通用方法之外，三个项目各有侧重，按场景叠加。

### 通用文案（公众号、小红书、产品文案）

用 zh-humanizer-literary 的思路：去 AI 味同时允许文采，但不要鸡汤。增加命名、意象、节奏和记忆点，让句子有辨识度。核心是「温暖平实，具体有判断」。

### 翻译润色

用 de-ai-tone 的翻译专项：拆关系子句、改被动句、删多余代名词。忠实优先于流畅，原文刻意的修辞不在清理之列。繁体场景额外做用词对照。

### 学术论文

用 de-AIGC-skill 的保量原则：以换句式、换词、移位为主，润色后字数不低于原文 92%。不新增事实、案例、数据。LaTeX 场景不误改 cite、ref、label 和数学环境。维护滚动摘要防止前后不一致。

## 五、为什么这些方法有效

AI 味的本质是统计惯性。模型在训练数据里见过太多某类表达，生成时会高概率复现。三个项目的方法都是同一逻辑：把高概率但非必要的表达识别出来，强制替换成低概率但更准确的表达。

所以方法不依赖任何特定模型或平台。只要中文 AI 还在产生这些统计惯性，这套清单就有效。当模型自身改进到不再产生这些惯性，清单自然失效，那时也不需要去 AI 味了。

## 六、局限

- 信号词表需要持续更新，模型迭代会换一批新套话
- 具体化依赖外部事实，没有事实素材时改写空间有限
- 学术场景的检测率降低不等于质量提升，保量润色主要是应对检测
- 繁简和地区用词差异需要单独处理，通用清单不覆盖

## 七、直接拿走用的最小版

如果只想要一份最小可执行清单，复制下面这段：

\`\`\`
1. 搜索并标记信号词：在当今时代、值得注意的是、综上所述、赋能、打造、具有重要意义
2. 命中的逐条判断，多数直接删
3. 找三项式排比，凑数的删第三条
4. 找段末总结句，多数删
5. 破折号换句号，修辞性设问改陈述
6. 抽象判断补具体依据，补不了就删
7. 列点改连续句子测试，更清楚就改
8. 删公式化开场和结尾
9. 产出前过一遍自检清单
\`\`\`

不需要装任何 Skill，任何能编辑文本的环境都能执行。

## 参考项目

- mengke-wang/zh-humanizer-literary：通用中文去 AI 味 + 文采增强，Codex Skill
- allenloves/de-ai-tone：繁体中文写作与翻译风格规范，Claude Code Skill
- zczjyq/de-AIGC-skill：学术论文降 AIGC 检测率，Cursor Skill

三个项目都在 GitHub 公开，各自 License 见仓库。`

const body = JSON.stringify({ title, content, tagNames: ['AI', 'Skills', 'Agent'] })
const data = Buffer.from(body, 'utf8')

const options = {
  hostname: '172.30.0.16',
  port: 35241,
  path: '/api/boot/forum/articles',
  method: 'POST',
  headers: {
    'Content-Type': 'application/json; charset=utf-8',
    'Content-Length': data.length,
    'Authorization': `Bearer ${TOKEN}`,
  },
}

const req = http.request(options, (res) => {
  let buf = ''
  res.on('data', (c) => (buf += c))
  res.on('end', () => {
    try {
      const j = JSON.parse(buf)
      console.log('code:', j.code)
      console.log('id:', j.data && j.data.id)
      console.log('title:', j.data && j.data.title)
      console.log('contentHead:', j.data && j.data.content.slice(0, 120))
      console.log('tags:', j.data && j.data.tags.map(t => t.tagName).join(', '))
    } catch (e) {
      console.log('parse error:', e.message)
      console.log('raw:', buf.slice(0, 500))
    }
  })
})
req.on('error', (e) => console.log('req error:', e.message))
req.write(data)
req.end()
